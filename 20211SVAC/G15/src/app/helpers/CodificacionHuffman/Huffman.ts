import { Node } from "./Node";

export class Huffman {
       Tree = null;
       root = null;
       Nodes=[]
       probs ={}
       dictEnconder={}

       constructor(symbols){
           this.initNodes(symbols);
         

       }

       initNodes(probs){
           let node: Node = new Node();
           for (let index = 0; index < probs.length; index++) {
               
               
           }
           

       }
}